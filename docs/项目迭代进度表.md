# 项目迭代进度表

## 当前迭代项

### 训练完成后的处理流程

当前训练任务采用异步提交 + 定时轮询的方式处理，整体状态流转如下：

`PENDING -> RUNNING -> VERIFYING_ARTIFACTS -> IMPORTING_SKILLS -> RELEASING -> SUCCEEDED / FAILED / TIMEOUT`

### 1. 提交训练任务

1. 创建训练任务记录，写入 `expert_training_task`
2. 状态初始化为 `PENDING`
3. 记录输入源清单 `source_manifest_json`
4. 计算训练输出目录 `output_dir`
5. `manifest_path` 当前存储为 JSON 数组字符串，格式如下：

```json
[
  {
    "name": "task-executor",
    "path": "/root/skills/2/session_xxx/generated-skills/task-executor"
  }
]
```

### 2. 提交代理训练

1. 异步线程将任务置为 `RUNNING`
2. 专家状态改为 `TRAINING`
3. 清理并重建训练输出目录
4. 根据输入源文件名推导最终 skill 目录名
5. 组装提示词后调用代理服务 `chat`
6. 将代理返回的 `sessionId`、`requestId` 回写到训练任务
7. 将 `lastTrainingSessionId` 回写到专家表

说明：

1. 提示词中的规范 skill 路径仅用于告诉代理按哪个规范生成
2. 最终产物判断只看 `output_dir` 对应的 skill 目录，不再用规范 skill 路径参与产物校验

### 3. 轮询训练会话是否结束

1. 定时任务按 `poll-interval-ms` 轮询训练任务
2. 只轮询状态为 `RUNNING`、`VERIFYING_ARTIFACTS`、`RELEASING` 的任务
3. `RUNNING` 阶段通过 `sessionId` 调用代理的“会话是否结束”接口
4. 当前会话阶段超时时间为 30 分钟
5. 轮询次数 `pollCount` 仅用于日志节流，不再作为超时判断依据

当前配置：

1. `poll-interval-ms = 5000`
2. `session-timeout-ms = 1800000`

### 4. 会话结束后的缓冲处理

1. 当代理接口返回“会话已结束”后，任务状态切换为 `VERIFYING_ARTIFACTS`
2. 不立即做后续动作
3. 固定等待 2 分钟，作为产物落盘缓冲时间
4. 缓冲期间只打印日志，不导入技能、不触发发布

当前配置：

1. `artifact-grace-period-ms = 120000`

### 5. 校验训练产物

缓冲时间结束后，开始检查训练输出目录中的 skill 产物：

1. 先读取训练任务的 `output_dir`
2. 如果 `manifest_path` 中已有 JSON 数组，则按其中的 `path` 读取技能目录
3. 如果 `manifest_path` 为空，则按系统推导的 skill 目录名去 `output_dir` 下定位目录
4. 要求技能目录存在
5. 要求技能目录下存在 `SKILL.md`
6. 校验通过后，将 `name + path` 以 JSON 数组形式回写到 `manifest_path`

说明：

1. 当前默认一次训练只处理一个 skill
2. `manifest_path` 的 JSON 数组结构已兼容后续一训多 skill 的扩展
3. 如果 2 分钟缓冲结束后产物仍未就绪，当前实现会直接标记训练失败，不再继续无限等待

### 6. 导入技能包

产物校验通过后，进入 `IMPORTING_SKILLS` 阶段，处理逻辑如下：

1. 将 skill 目录压缩为 zip
2. 从 zip 中读取 `SKILL.md`
3. 解析 `name` 和 `description`
4. 向 `skill_package` 表插入一条技能包记录
5. 将 zip 文件写入共享存储目录
6. 回写 `package_path`

当前 zip 中 `SKILL.md` 的识别规则：

1. 优先读取 zip 根目录下的 `SKILL.md`
2. 如果根目录没有，再兜底读取任意子目录下的 `.../SKILL.md`

### 7. 替换专家技能绑定

skill 导入成功后：

1. 删除当前专家已有的 `expert_skill_binding`
2. 将本次训练导入的 skill 重新绑定到专家
3. 当前排序按导入顺序写入 `sort_no`

### 8. 触发专家发布

技能导入并绑定完成后：

1. 训练任务状态切换为 `RELEASING`
2. 调用发布服务创建发布任务
3. 将 `releaseTaskId` 回写到训练任务表
4. 后续轮询发布任务状态

当前发布阶段超时时间为 30 分钟：

1. `release-timeout-ms = 1800000`

### 9. 训练成功时的回写动作

当发布任务成功后，训练任务才真正结束，处理如下：

1. 训练任务状态改为 `SUCCEEDED`
2. `artifactVerified = 1`
3. 回写 `finishedAt`
4. 专家表回写 `lastTrainingTaskId`
5. 专家表回写 `lastTrainingSessionId`
6. 专家表的 `trainingVersion + 1`

### 10. 训练失败或超时处理

以下情况会导致任务失败：

1. 提交代理失败
2. 会话轮询超时
3. 缓冲结束后仍未找到有效 skill 产物
4. 技能包导入失败
5. 发布任务失败
6. 发布任务轮询超时

失败或超时后的处理：

1. 训练任务状态置为 `FAILED` 或 `TIMEOUT`
2. 回写 `failureReason`
3. 回写 `finishedAt`
4. 恢复专家训练前状态
5. 回写专家的 `lastTrainingTaskId`
6. 如果已有 `sessionId`，同时回写 `lastTrainingSessionId`

## 当前实现重点说明

### 规范 skill 与训练产物的边界

1. 规范 skill 路径只用于提示代理按既定规范生成 skill
2. 最终训练成功的判断只基于 `output_dir` 下的实际产物目录
3. 技能包解析时不再使用 `skills/root-skills-creator/SKILL.md` 这类规范路径判断最终产物

### 当前已落地的时间策略

1. 会话轮询最长 30 分钟
2. 会话结束后固定缓冲 2 分钟
3. 发布轮询最长 30 分钟
4. 轮询次数只用于打印进度日志

### 当前已落地的产物策略

1. 当前一次训练默认只生成一个 skill
2. skill 目录名默认按输入源文件名推导
3. 训练成功后会自动入库 `skill_package`
4. 训练成功后会替换专家当前绑定技能
5. 最终训练成功以“发布成功”为准

## 本轮迭代补充（2026-04-02）

### 1. 训练源能力补充

1. 保留原有逆向训练链路：`/api/v1/file/uploadAndTrain -> app_info 解压 -> LOCAL_PATH 训练`
2. 新增 Git 正向训练能力，训练源支持 `GIT_URL + sourceVersion`
3. Git 训练与逆向训练提示词已拆分为两套独立实现，互不复用提示词正文

### 2. AI Agent 模型演进

1. AI Agent 已提升为独立主实体，新增 `de_ai_agent`
2. Agent 与 skill、expert 的关系分别使用 `de_agent_skill_binding`、`de_agent_expert_binding`
3. JavaAgent 上报型逆向训练支持按 `de_ai_agent.auto_binding=1` 自动绑定专家并刷新 Agent 技能目录

### 3. 训练失败收尾策略

1. 训练失败或超时后，保留训练任务记录，不删除训练源
2. 仅清理当前任务输出目录 `outputDir`
3. 如果专家当前尚无任何已导入技能，则继续清理空的技能根目录，避免影响下次重训
4. 专家状态仍按原逻辑恢复，不影响成功链路

### 4. 当前待继续优化项

1. 训练任务排队目前仍主要依赖应用内线程池，尚未接入 Redis / MQ 级别的持久化排队
2. 训练会话结束判断仍采用定时轮询，后续需评估延迟队列或回调方案
3. 大训练源场景下，仍需继续优化任务调度、超时控制和存储清理策略

## 本轮迭代补充（2026-04-02 / 专家列表查询增强）

### 1. 查询条件增强

1. `/api/v1/experts/list` 新增可选字段 `appNames`
2. 支持按 `appName` 集合过滤专家
3. 当 `names` 与 `appNames` 同时传入时，按组合条件过滤结果

### 2. 返回字段增强

1. 专家列表返回新增 `trainingInProgress`
2. 该字段按专家当前是否存在活跃训练任务实时计算
3. 不改变原有 `status` 字段语义，`status` 仍表示专家自身状态

## 本轮迭代补充（2026-04-02 / 训练任务中止能力）

### 1. 代理能力接入

1. 已接入代理新增接口 `ABORT_CONVERSATION`
2. 后端可按 `sessionId` 主动中止训练会话

### 2. 训练任务中止接口

1. 新增 `/api/v1/experts/training-tasks/abort`
2. 支持对 `PENDING`、`RUNNING`、`VERIFYING_ARTIFACTS` 状态的训练任务执行中止
3. 中止时会尝试停止代理会话，并清理本次训练输出目录
4. 中止完成后会恢复专家状态，并删除当前训练任务记录

## 本轮迭代补充（2026-04-03 / 专家列表状态过滤）

### 1. 查询条件增强

1. `/api/v1/experts/list` 新增可选字段 `status`
2. 支持按专家状态过滤列表结果
3. 可与 `names`、`expertType`、`appNames` 组合使用

## 本轮迭代补充（2026-04-03 / 平台服务列表过滤）

### 1. 查询规则调整

1. `/api/v1/agents/services/list` 中的专家数据改为只返回 `STARTED` 状态专家
2. AI Agent 仍保持原有查询逻辑
3. 返回结果中 `SERVICE` 类型现在仅表示已启动可用专家

## 新增能力说明（2026-04-03 / 会话问答接口）

### 1. 新增问答会话相关接口
1. 新增 `/api/v1/conversations/messages/send`，支持显式传入或自动生成 `sessionId`，用于串联消息。
2. 新增 `/api/v1/conversations/messages/list`，用于按 `sessionId` 获取历史消息列表。
3. 新增 `/api/v1/conversations/finished`，用于查询会话是否已完成。
4. 新增 `/api/v1/conversations/abort`，用于中止当前会话。

### 2. 新增配置会话代理能力
1. 新增 `digital-expert.conversation` 配置项，替代原有的 `digital-expert.proxy` 配置。
2. 默认基地址为 `http://127.0.0.1:4096`，支持覆盖 `providerId`、`modelId`、`agent`、`system`、`variant`。
3. 会话过程会按 `sessionId` 复用 `directory/workspace`，不存在时再创建新的。

### 3. 问答接入方式调整
1. 会话问答能力改为通过独立的 `opencode-spring-boot-starter` 提供，避免业务项目重复维护 OpenCode HTTP 客户端实现。
2. 会话接口统一收敛到 `/api/v1/conversations` 路由下沉，只保留底层通用能力。
3. 业务项目改为使用 `opencode.proxy` 前缀进行会话接入，复用默认模型配置。

## 本轮迭代补充（2026-04-07 / 专家别名字段）
### 1. 专家表字段扩展
1. 专家领域模型新增 `aliasName` 字段，对应数据库列 `alias_name`。
2. 当前库迁移 SQL 已补到 `docs/sql/20260407_add_alias_name_to_de_digital_expert.sql`。
3. 专家摘要与专家配置返回已同步带出别名字段。

### 2. 赋值规则调整
1. 手动新增专家时，别名默认与专家名称一致。
2. Git 正向训练新建或复用专家时，别名统一与专家名称一致，描述继续使用传入的描述。
3. 逆向训练自动建专家时，当前描述本来就使用服务名，因此别名也与服务名保持一致。
4. 对于旧的逆向专家记录，如果别名为空，后续再次命中自动解析链路时会自动补齐别名。

## 本轮迭代补充（2026-04-07 / 平台服务列表别名展示）
### 1. 列表返回规则调整
1. `/api/v1/agents/services/list` 在返回 `SERVICE` 类型数据时，`description` 字段改为优先返回专家 `aliasName`。
2. 当专家别名为空时，仍回退使用原有 `description` 字段，避免旧数据出现空描述。
3. `AGENT` 类型返回逻辑保持不变。

## 本轮迭代补充（2026-04-07 / 专家包导入）
### 1. 新增导入能力
1. 新增 `POST /api/v1/experts/package/import`，支持上传已导出的专家 ZIP 包并导入平台。
2. 导入时会自动解析 `expert-config.json`、`skills/`、`static_packages/`，复用现有技能包、静态资源包和专家绑定能力完成落库。
3. 支持通过 `autoRelease` 参数控制导入后是否立即触发专家发布。

### 2. 导入处理规则
1. 技能目录会在导入时重新压缩为 skill zip，再复用现有 skill 上传流程入库。
2. 静态资源包会按导出包中的文件重新上传并绑定到新专家。
3. 导入会同步恢复专家别名与 MCP 绑定，失败时会清理本次导入生成的包文件和临时目录。

## 本轮迭代补充（2026-04-07 / 导入专家包后的 Agent 刷新）
### 1. 导入发布后的补偿处理
1. 专家包导入不会经过训练任务后置处理链路，因此新增了导入专属的发布完成补偿。
2. 当导入接口开启 `autoRelease=true` 时，会在发布成功后手动补一次 `refreshActiveAgentsByExpert`。
3. 补偿逻辑通过发布任务查询结果判断是否成功，尽量避免导入逻辑直接耦合发布实现细节。

## 本轮迭代补充（2026-04-07 / AI Agent OpenCode 运行时配置）
### 1. Agent 运行时配置扩展
1. AI Agent 模型新增 `root_path` 与 `opencode_config_json` 字段，用于维护项目根目录与 OpenCode 基础配置模板。
2. 新增独立迁移 SQL：`docs/sql/20260407_add_root_path_and_opencode_config_json_to_de_ai_agent.sql`。
3. Agent 创建与详情返回已同步带出根目录和 OpenCode 基础配置内容。

### 2. opencode.json 生成与刷新
1. 新增独立的 `AgentRuntimeConfigService`，专门负责聚合专家 MCP 并生成 `root_path/opencode.json`。
2. 新增 `AgentOpencodeConfigPostProcessor`，在训练成功后单独刷新关联 Agent 的 OpenCode 运行时配置，与技能部署解耦。
3. Agent 绑定变更、Agent 启停、导入专家包发布完成后，也会补充刷新对应 Agent 的 `opencode.json`。
4. 多专家聚合同名 MCP 时，若配置完全一致则去重；若配置冲突则记录告警并保留先出现的绑定配置。

## 本轮迭代补充：2026-04-08 / AI Agent 专家绑定专用接口
### 1. 新增只绑定专家接口
1. 新增 `POST /api/v1/agents/expert-bindings/update`，用于 AI Agent 单独维护绑定的专家列表。
2. 新接口只更新 `de_agent_expert_binding`，不会覆盖 Agent 已有的直接 skills 绑定。
3. 绑定更新完成后，仍会统一刷新 Agent 技能目录与 `opencode.json`。

## 本轮迭代补充：2026-04-08 / 训练超时配置改为 Duration
### 1. 训练时长配置可读性优化
1. `digital-expert.training.session-timeout` 改为 `Duration` 配置，支持直接写成 `30m`。
2. `digital-expert.training.artifact-grace-period` 改为 `Duration` 配置，支持直接写成 `2m`。
3. `digital-expert.training.release-timeout` 改为 `Duration` 配置，支持直接写成 `30m`。
4. 训练服务内部统一在使用时转换为毫秒，业务逻辑不变。

## 本轮迭代补充：2026-04-09 / 训练产物缓冲改造
### 1. 产物缓冲语义调整
1. `artifact-grace-period` 不再作为固定等待时长，而是作为训练产物的最大等待时间。
2. 进入 `VERIFYING_ARTIFACTS` 后，会优先检测技能根目录 `SKILL.md` 是否已生成。
3. 检测到 `SKILL.md` 后会立即尝试校验并导入产物，不再强制等满缓冲时间。
4. 若 `SKILL.md` 已生成但版本目录或 `static_package` 仍未完全就绪，则继续轮询等待，直到超过最大等待时间才失败。

## 本轮迭代补充：2026-04-09 / 专家来源字段与删改限制
### 1. 新增专家来源 expertSource
1. 专家模型新增 `expertSource` 字段，用于区分 `CREATED` 与 `GENERATED`。
2. 手工创建、导入专家包、正向训练新建专家默认记为 `CREATED`。
3. 逆向自动创建的专家记为 `GENERATED`，并在列表与详情接口中一并返回。

### 2. 删改限制
1. 专家绑定更新、删除、启停状态变更仅允许 `expertSource=CREATED` 的专家执行。
2. 非 `CREATED` 专家会返回 `EXPERT_NOT_EDITABLE`，避免仅靠前端控制导致误修改。
3. 新增迁移脚本 `docs/sql/20260409_add_expert_source_to_de_digital_expert.sql`，用于补充字段并回填历史数据。

## 本轮迭代补充：2026-04-09 / 专家基础信息修改接口
### 1. 新增修改专家接口
1. 新增 `POST /api/v1/experts/update`，用于修改专家名称、别名、描述、提示词、专家类型。
2. 修改接口仅允许 `expertSource=CREATED` 的专家使用，`GENERATED` 专家仍保持只读。
3. 为避免改名或改类型时与训练/发布流程冲突，存在进行中训练或发布任务的专家暂不允许修改。
## 2026-04-09 Kafka 接入骨架
- expert-web 新增 spring-kafka 依赖与 Kafka 消费基础配置，增加 digital-expert.kafka.enabled 开关与 dc_databuff_k8s 主题配置。
- 新增 dc_databuff_k8s 主题监听器，当前只负责消费消息并提取 serviceVersion，不接入训练主流程。
- 新增 KafkaServiceVersionService，先按通用 JSON 递归查找 serviceVersion，后续可根据真实消息样例继续收紧解析规则。
## 2026-04-09 Kafka serviceVersion 骨架调整
- Kafka serviceVersion 提取服务去掉 ObjectMapper 依赖，统一改为 fastjson2 基础能力。
- 当前先保留消费骨架，不预设消息结构，也不提前编写 serviceVersion 解析规则，等待真实样例后再补充。
## 2026-04-09 Kafka 版本快照入库
- 新增 de_service_version_snapshot 表，独立保存 Kafka dc_databuff_k8s 主题中的服务版本快照，不影响现有专家训练链路。
- Kafka 消息按 data.containerStatuses[].image 提取镜像 tag 作为 service_version，并按 cluster_id + namespace + pod_name + container_name 维度做更新入库。
- 额外保存 app_name / workload_name / image_name / raw_payload / last_seen_at，后续可直接按app_name 查询最新版本，替换当前逆向训练里的 pp.json.serviceVersion 来源。
## 2026-04-09 Kafka 监听器容器工厂修复
- 显式补充 kafkaListenerContainerFactory Bean，避免启用 Kafka 消费时启动阶段找不到默认监听容器工厂。
- 该修复只影响 Kafka 监听器装配，不改变既有消费与版本快照入库逻辑。
## 2026-04-09 Kafka ConsumerFactory 显式配置
- Kafka 配置类显式补充 ConsumerFactory<String, String>，不再依赖 Spring Boot Kafka 自动装配是否生成默认消费工厂。
- 与 kafkaListenerContainerFactory 一起组成完整监听器基础设施，解决启动阶段缺少 ConsumerFactory Bean 的问题。
## 2026-04-09 Kafka 消息类型过滤
- Kafka 版本快照入库逻辑新增 	ype=41 过滤，只有该类型的消息才继续解析 containerStatuses[].image 并入库。
- 其他类型消息直接忽略，不影响现有版本快照表数据。
- 2026-04-10：统一补齐 `.claude` 与 `.codex` 的 `product-rules` 技能，明确本仓库的产品规则、实现边界和协作约束；新增禁止主动引用 `root-skills-creator`、不扩展 `auto_binding`、需求完成后必须更新迭代进度表等项目级规范。
- 2026-04-10：放开专家删除来源限制，`deleteExpert` 不再校验 `expertSource=CREATED`；保留训练中、发布中的拦截和目录/绑定清理逻辑，同时将只读限制提示收敛为“暂不允许修改、绑定或变更状态”。
- 2026-04-10：逆向训练版本目录解析接入 `de_service_version_snapshot`，当前优先级调整为 `sourceVersion > 按专家 app_name 查询最新快照版本 > app.json.serviceVersion > 自动 vN`；查不到快照版本时保持原有回退逻辑，不新增额外配置项。
- 2026-04-10：新增会话列表接口 POST /api/v1/conversations/sessions/list，基于 de_conversation_session 倒序返回最近会话的 sessionId、标题、模型、状态、消息数、同步时间等基础信息，默认沿用现有 ConversationMysqlService 主链路，不额外触发远端同步。

- 2026-04-10：逆向训练查 de_service_version_snapshot 获取版本时，改为优先使用专家 lias_name 匹配，pp_name 仅作为兜底，避免原始应用名包含 :: 前缀导致查不到 Kafka 快照版本。

## 2026-04-11 上传入口 Redis 去重与异步受理
- `POST /api/v1/file/uploadAndTrain` 改为只返回受理结果 `Boolean`，上传请求不再同步等待解压与触发训练完成。
- 新增基于 Redis 的同应用入口锁，按上传文件名解析出的应用名做去重；同一应用已在上传链路中或已存在进行中的训练任务时，接口直接返回成功并记录日志，不再向调用方暴露“存在进行中的训练任务”。
- 上传入口新增独立异步执行器，当前仅将“解压 app_info + 自动触发训练”挪到后台执行；训练主状态机、轮询、发布链路保持不变。
- 补充 `spring.data.redis` 基础配置与空的 `app-upload-schema.sql` 占位资源，避免默认配置引用缺失资源导致启动异常。

### 后续迭代计划
- 继续评估是否需要把同应用去重键从文件名应用名升级为更稳定的业务标识，但现阶段先保持最小范围实现。
- 若后续上传洪峰仍明显，再在此基础上增加 Redis 队列或全局上传并发控制，不直接重构训练主流程。
## 2026-04-12 Redis 入口锁改为独立 Redisson 配置
- 上传入口应用去重锁从 `spring.data.redis` 自动装配切换为项目内自定义 `digital-expert.redis.*` 配置，不再依赖 Spring Boot 默认 Redis 属性前缀。
- 新增 `RedissonConfiguration`，统一构造 `RedissonClient`，当前按单节点 Redis 模式接入，满足后续架构调整时由业务配置独立演进。
- `AppUploadGuardService` 改为基于 Redisson 实现入口锁的加锁与释放，业务语义保持不变：同应用重复上传仍直接返回成功并记日志。
## 2026-04-13 健康检查接口日志过滤
- `ApiRequestLogAspect` 新增健康检查短路逻辑，对 `HealthController` 的 `/health` 请求不再打印 API 请求、响应和异常日志。
- 过滤范围仅限健康检查接口，其余控制器日志行为保持不变。
## 2026-04-14 前端 history 路由刷新修复
- 新增 `SpaRouteController`，对 `/chat` 这类无扩展名的前端路由请求统一转发到 `index.html`，避免浏览器刷新时被 Spring 当成静态资源查找。
- 保留 `/api/**`、`/health`、`/assets/**`、`/webjars/**` 等后端与静态资源路径，不影响现有接口和资源访问。
- `GlobalExceptionHandler` 新增 `NoResourceFoundException` 与 `ResponseStatusException` 处理，真实的资源不存在场景改为返回 404，不再统一落到 `5000001`。
## 2026-04-14 专家追加绑定多个智能体
- 新增 `POST /api/v1/experts/agent-bindings/bind`，仅支持将单个专家追加绑定到多个智能体，不覆盖智能体现有专家绑定关系。
- 绑定逻辑按请求中的 `agentIds` 去重并校验存在性；若某个智能体已绑定该专家则跳过，保持幂等。
- 对新增绑定的智能体会立即刷新技能部署与 `opencode.json`；同时补充专家发布成功后的关联智能体刷新，避免专家从 `DRAFT` 发布为 `STARTED` 后绑定关系不生效。
## 2026-04-15 手工创建专家接入异步智能体绑定
- `/api/v1/experts/manual/create` 新增可选 `agentIds` multipart 参数，支持在创建专家时一并指定要追加绑定的智能体列表。
- 创建接口在主事务提交后异步触发专家绑定多个智能体，复用已有 `agent-bindings/bind` 追加绑定能力，不阻塞专家创建响应。
- 异步绑定失败仅记录日志，不回滚已完成的专家创建、技能上传与发布任务提交流程；若专家后续发布成功，关联智能体仍会再次刷新并最终生效。
## 2026-04-15 逆向训练专家描述回填
- 逆向训练产物导入技能包后、提交发布任务前，新增自动生成专家描述回填逻辑。
- 仅对 `expertSource=GENERATED` 的专家生效，且当前描述为空或仍等于专家名称/别名时才使用本次导入的首个技能包描述覆盖。
- 描述来源改为训练产物 `SKILL.md` 中落库到 `skill_package.description` 的内容，避免逆向专家长期仅使用服务名作为描述。
## 2026-04-16 删除专家清理孤儿资源包
- `deleteExpert` 删除专家前会先记录当前绑定的 `skillId` 和 `staticPackageId`，删除专家与绑定关系后再按引用关系清理孤儿技能包、静态资源包及其共享存储目录。
- 技能包清理增加引用保护：若该 `skillId` 仍被其他专家绑定，或仍被 AI Agent 直接绑定，则跳过删除，避免误删共享技能资源。
- 专家删除后对受影响 AI Agent 同时刷新技能部署与 `opencode.json`，保证绑定移除后的运行时配置也能及时收敛。
## 2026-04-16 手工创建专家 agentIds 传参改为 JSON
- `/api/v1/experts/manual/create` 的智能体绑定参数由重复表单字段 `agentIds` 调整为单个表单字段 `agentIdsJson`，调用方可直接传 JSON 数组字符串。
- Controller 层新增 `agentIdsJson` 解析逻辑，统一转为 `List<Long>` 传入原有手工创建专家与异步绑定链路，Service 层逻辑保持不变。
- 当 `agentIdsJson` 非法时返回 `INVALID_REQUEST`，提示“智能体 ID JSON 格式不正确”。
## 2026-04-16 SKILL.md 头部引号清洗
- 技能包元数据解析新增统一清洗逻辑：当 `SKILL.md` 顶部 `name` 或 `description` 被成对单引号或双引号包裹时，入库前自动剥离外层引号。
- 修复范围位于 `ZipArchiveService.inspectSkillArchive(...)` 解析链路，手工上传技能包与训练产物导入技能包都会同时生效。
- 仅剥离首尾成对包裹引号，不会改动正文中的内部引号内容，避免误伤正常描述文本。
## 2026-04-17 手工专家一站式修改
- 新增 `POST /api/v1/experts/manual/update` multipart 接口，统一支持修改手工专家基础信息，并按需上传新 `skillFiles`、替换 MCP 绑定。
- 修改接口复用现有 `updateExpert`、技能包上传和专家绑定持久化逻辑：未传 `skillFiles` 则保留原技能，未传 `mcpsJson` 则保留原 MCP，传空数组 `[]` 才表示清空 MCP。
- 只有技能或 MCP 实际发生变化且专家当前状态为 `STARTED` 时，才复用现有异步发布任务链路重新发布专家；若只是改名称、描述、Prompt 等基础信息，则不触发发布，也不刷新关联 Agent。
- 若替换了技能包，会在绑定切换完成后清理被移除且无其他引用的孤儿技能包，避免手工专家多次编辑后堆积无用技能资源。
## 2026-04-20 专家图标上传与返回
- 新增 `POST /api/v1/files/upload-image` 通用图片上传接口，支持 `png`、`jpg`、`jpeg`、`webp`，图片保存到共享存储 `uploads/images/yyyyMMdd`，返回可访问的 `url`。
- 新增 `GET /api/v1/files/public/images/{date}/{fileName}` 图片访问接口，按上传生成的日期目录和文件名读取共享存储中的图片资源。
- `de_digital_expert` 新增 `icon_url` 字段，手工创建、手工修改、普通创建、普通修改、专家列表、专家详情和发布配置均透传 `iconUrl`，不改变技能包、MCP、发布和 Agent 刷新主链路。
- 补充迁移脚本 `docs/sql/20260420_add_icon_url_to_de_digital_expert.sql`，用于已有库手动增加 `icon_url` 字段。

## 2026-04-21 root-skills-creator 融入指标体系元数据
- 约定训练时从 `/app/upload/data` 读取指标体系元数据（2026-04-21 起由原先子路径 `.../metrics` 收敛为 `.../data` 根目录，下同）；更新 `root-skills-creator/SKILL.md`：主 Agent 预加载与按环节裁剪下发、子 Agent 强制融合黄金/非黄金指标、与源码采集点冲突时的合并规则、巡检与问数须与元数据对齐。
- 更新模板：`02-领域认知`（整体架构、整体逻辑、子环节）、`04-场景能力/巡检清单.md`、`问数语义.md`（新增业务语义→指标映射章节）、`SKILL-template.md` 场景路由与问数/巡检约束；`03-实体模型` 保持仅从领域认知归纳相关指标，不绑定 `/app/upload/data` 元数据。
- 交付文档正文不写入元数据目录等内部路径；问数约束增加映射须有元数据或领域认知依据。
- 领域认知模板与 `SKILL.md` 同步调整：`02-领域认知` 交付物不再要求「代码位置」、源码路径与行号，指标采集点统一为 `类.方法` 逻辑锚点；读源码仍用于生成，但正文不落文件路径与行号。
## 2026-04-22 训练附件资源可插拔接入
- 新增 `de_attachment_resource` 附件资源表及 `POST /api/v1/attachments/list` 查询接口，附件可按 `scope/status/sortNo` 管理，逆向默认附件使用 `scope=REVERSE_DEFAULT`。
- 训练链路新增 `TrainingAttachmentProvider` 扩展点：正向训练通过 `attachmentIdsJson` 选择附件，逆向训练自动加载默认附件，附件以 `ATTACHMENT` 角色写入训练源清单，不新增任务关系表。
- `AppInfoTrainingPromptStrategy` 与 `GitTrainingPromptStrategy` 会动态追加“附件资源”提示段，训练代理提交的 `filePaths` 同步包含附件存储路径；主输入源、版本目录解析和技能目录解析会忽略附件源，避免影响原训练主流程。
## 2026-04-23 逆向训练元数据清洗
- `root-skills-creator/SKILL.md` 去掉 OpenCode / `opencode.json` 等绑定表述，子 Agent 委派统一描述为宿主 **task** 与平台等价能力，便于底层运行时替换。
## 2026-04-23 指标核心表查询与资源生成
- 新增外部固定表 `dc_databuff.dc_databuff_metrics_core` 的只读查询能力，提供 `POST /api/v1/metrics-core/list` 明细查询和 `POST /api/v1/metrics-core/categories` 按 `type1/type2/type3` 分类查询。
- 新增 `POST /api/v1/metrics-core/resources/generate`，按 `type1` 分组把启用指标展开为 JSON 文件，文件内容形如 `{ "measurement:desc": "fieldKey:field.describe" }` 的数组。
- 生成文件写入共享存储 `metric_resources/` 目录，并同步为 `de_attachment_resource` 中的 `GENERAL` 附件资源；该逻辑独立于训练提交，不会自动影响正向或逆向训练流程。