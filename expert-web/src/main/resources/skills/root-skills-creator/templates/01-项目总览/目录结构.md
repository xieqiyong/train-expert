# {项目名}目录结构

## 根目录结构

```
{项目根目录}/
├── {模块1}/           # {职责说明}
│   └── ...
├── {模块2}/           # {职责说明}
│   └── ...
├── config/            # 配置文件
├── docs/              # 文档
└── ...
```

## 核心源码目录

```
src/main/java/com/{公司名}/{项目名}/
├── {模块1}/           # {职责}
├── {模块2}/           # {职责}
├── common/            # 公共组件
├── config/            # 配置类
└── util/              # 工具类
```

## 关键文件说明

| 文件路径 | 说明 | 重要性 |
|---------|------|-------|
| {文件1} | {说明} | ⭐⭐⭐ |
| {文件2} | {说明} | ⭐⭐ |

## 配置文件位置

| 配置类型 | 文件位置 | 说明 |
|---------|---------|------|
| 应用配置 | {路径} | {说明} |
| 日志配置 | {路径} | {说明} |
