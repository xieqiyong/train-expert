>> **子 Agent 执行提示**：你当前是一个独立的子 Agent，专门负责**这一个环节**。你的上下文预算全部用于该环节，必须做到：1）用 `find` 命令探测源码目录；2）全量读取入口层 + 追踪调用链到边界；3）生成深度不低于模板示例的文档；4）写完后执行 `find` 命令校验文件数量和子环节对齐。
>> **指标体系元数据**：写稿前须结合主 Agent 下发的摘录，并自行用文件工具核对 `/app/upload/data` 下与本环节相关的文件。**本文件与每个子环节 `.md` 均须包含** `## 黄金指标` 与 `## 非黄金指标` 章节。表格**首列「指标 identifier」**须与元数据/摘录中的**原始 identifier 逐字一致**，**禁止**截断或简称。子环节与元数据须一致；采集点用 **`类.方法`** 等逻辑锚点表述即可，**不写源码行号与文件路径**。无元数据命中时于各节中注明，指标仍按模板拆分黄金/非黄金，并标注「源码推断」。

# {环节名称}整体逻辑

## 环节概述

### 基本信息
| 属性 | 内容 |
|-----|------|
| **环节名称** | {环节名称} |
| **所属系统** | {项目名} |
| **环节层级** | L3 - 环节级 |
| **模块归属** | {业务/包级语义名，如 order-service，不写具体文件路径} |
| **代表性入口** | `{主类名}.{主方法名}`（仅类.方法，用于定位逻辑，非文件路径） |

### 核心功能
{详细描述该环节的核心功能和主要职责，包括：}
- **主要责任**：{该环节负责的核心业务}
- **辅助责任**：{次要但相关的业务}
- **边界责任**：{明确不包含的职责，避免与相邻环节混淆}

### 职责范围
{详细说明该环节负责的具体业务范围或功能领域，可以包括：}
- **功能边界**：{该环节处理什么，不处理什么}
- **数据边界**：{该环节管理哪些数据，不管理哪些}
- **接口边界**：{提供什么接口，调用什么接口}

### 输入输出

#### 输入数据
| 输入名称 | 输入类型 | 来源环节/系统 | 数据格式 | 传输协议 | 频率 |
|---------|---------|-------------|---------|---------|-----|
| {输入1} | {请求/事件/数据} | {来源} | {JSON/Protobuf/...} | {HTTP/gRPC/MQ} | {实时/批量} |
| {输入2} | {类型} | {来源} | {格式} | {协议} | {频率} |

#### 输出数据
| 输出名称 | 输出类型 | 目标环节/系统 | 数据格式 | 传输协议 | 方式 |
|---------|---------|-------------|---------|---------|-----|
| {输出1} | {响应/事件/数据} | {目标} | {格式} | {协议} | {同步/异步} |
| {输出2} | {类型} | {目标} | {格式} | {协议} | {方式} |

### 在架构中的位置

```
[{上游环节1}] ──{数据流}──┐
                         ↓
[{上游环节2}] ──{数据流}──→ [{当前环节}] ──{数据流}──→ [{下游环节1}]
                         ↓                           ↓
                         └─{数据流}──→ [{下游环节2}] ──{数据流}──→ [{下游环节3}]
```

**上下游关系详表**：
| 关系 | 环节/系统 | 交互方式 | 交互内容 | 依赖强度 |
|-----|---------|---------|---------|---------|
| 上游 | {上游1} | {方式} | {内容} | {强/弱} |
| 上游 | {上游2} | {方式} | {内容} | {强/弱} |
| 下游 | {下游1} | {方式} | {内容} | {强/弱} |
| 下游 | {下游2} | {方式} | {内容} | {强/弱} |

---

## 黄金指标

> **本环节全量**（与主 Agent 下发及 `/app/upload/data` 中**本环节 + 黄金** 的 identifier 集合一致；**禁止**缺行、**禁止**截断 identifier）：

| 指标 identifier（元数据原样） | 指标作用说明 | 采集点 | 正常范围 | 异常判定 | 告警级别 |
|-----------------------------|-------------|-------|---------|---------|---------|
| {id_golden_1} | {说明} | `{类名}.{方法名}` | {范围} | {判定} | {P0/P1/P2} |
| {id_golden_2} | {说明} | `{类名}.{方法名}` | {范围} | {判定} | {P0/P1/P2} |
| {id_golden_3} | {说明} | `{类名}.{方法名}` | {范围} | {判定} | {P0/P1/P2} |

## 非黄金指标

> **本环节全量**（与**本环节 + 非黄金** 的 identifier 集合一致；**禁止**截断）：

| 指标 identifier（元数据原样） | 指标作用说明 | 采集点 | 用途 | 正常范围 |
|-----------------------------|-------------|-------|------|---------|
| {id_aux_1} | {说明} | `{类名}.{方法名}` | {用途} | {范围} |
| {id_aux_2} | {说明} | `{类名}.{方法名}` | {用途} | {范围} |
| {id_aux_3} | {说明} | `{类名}.{方法名}` | {用途} | {范围} |

### 业务指标（可选）

| 指标 identifier 或业务名 | 业务含义 | 计算方式 | 用途 |
|-------------------------|---------|---------|------|
| {id_or_name} | {含义} | {计算} | {用途} |

---

## 子环节与黄金指标

> **指标清单完整性**：子环节级「黄金指标」与「非黄金指标」须覆盖 `/app/upload/data` 中划归本环节或本子环节的条目；**identifier 须与元数据逐字一致、禁止截断**。若元数据将同一指标挂在多个子环节，在各自文档中均可列示并注明「同指标多采集面」。用户交付文档中禁止出现内部元数据目录路径。

> **物理阻断式生成要求（核心约束）**：
> 本节表格中**每写一行子环节**，你都必须**立即停止继续写 `整体逻辑.md`**，转身去生成对应的 `{子环节}.md` 文件。只有该子环节的 `.md` 文件写完后，你才允许回到本文件继续写下一个子环节。
>
> **禁止行为**：
> - 禁止先把本节的 3-5 个子环节全部写完，再回头统一生成子环节文件
> - 禁止在表格中声明了子环节但同目录下没有对应的 `.md` 文件
> - 若暂未拆分子环节，写完本节后必须立刻在同一上下文中生成至少 2 份细节文档

### 子环节总览

| 子环节名称 | 核心职责 | 对应文档 | 复杂度 | 风险等级 |
|-----------|---------|---------|-------|---------|
| {子环节1} | {职责} | `{子环节1}.md` | {高/中/低} | {P0/P1/P2} |
| {子环节2} | {职责} | `{子环节2}.md` | {高/中/低} | {P0/P1/P2} |
| {子环节3} | {职责} | `{子环节3}.md` | {高/中/低} | {P0/P1/P2} |

### 子环节1：{子环节名称}

#### 职责说明
- **核心功能**：{详细描述}
- **业务价值**：{解决什么问题}
- **触发条件**：{什么时候执行}
- **执行频率**：{实时/定时/事件驱动}

#### 黄金指标（1-5个，identifier 与元数据逐字一致）

| 指标 identifier（元数据原样） | 指标作用说明 | 采集点 | 正常范围 | 异常判定 | 告警级别 |
|-----------------------------|-------------|-------|---------|---------|---------|
| {id_golden_1} | {说明} | `{类}.{方法}` | {范围} | {判定} | {P0/P1} |
| {id_golden_2} | {说明} | `{类}.{方法}` | {范围} | {判定} | {P0/P1} |
| {id_golden_3} | {说明} | `{类}.{方法}` | {范围} | {判定} | {P1/P2} |

#### 性能指标
| 指标名 | 目标值 | 警告阈值 | 严重阈值 | 说明 |
|-------|-------|---------|---------|------|
| 平均响应时间 | {X}ms | {X}ms | {X}ms | {说明} |
| P99响应时间 | {X}ms | {X}ms | {X}ms | {说明} |
| 吞吐量 | {X}TPS | {X}TPS | {X}TPS | {说明} |
| 错误率 | <{X}% | {X}% | {X}% | {说明} |

#### 对应文档
**详细文档**：`{子环节名称}.md`

### 子环节2：{子环节名称}

#### 职责说明
- **核心功能**：{描述}
- **业务价值**：{说明}
- **触发条件**：{条件}
- **执行频率**：{频率}

#### 黄金指标
| 指标 identifier（元数据原样） | 指标作用说明 | 采集点 | 正常范围 | 异常判定 | 告警级别 |
|-----------------------------|-------------|-------|---------|---------|---------|
| {id_golden_1} | {说明} | `{类}.{方法}` | {范围} | {判定} | {P0/P1} |

#### 对应文档
**详细文档**：`{子环节名称}.md`

---

## 核心数据流程

### 流程1：{流程名称}

#### 流程概述
- **业务目的**：{该流程解决什么问题}
- **触发条件**：{什么时候触发}
- **参与子环节**：{子环节1} → {子环节2} → {子环节3}
- **预期耗时**：{正常情况下的完成时间}
- **超时时间**：{设置的超时阈值}

#### 流程图

> **流程深度要求**：禁止只画 3-5 个高层阶段框，必须拆解到「子环节内的关键操作/方法级别」，并显式标注分支、异常、指标、配置参数。

```
[数据输入: {输入格式}]
    │  字段: {field1}, {field2}
    │  来源: {上游环节}
    ↓
[{子环节1}: {类名}.{方法名}]
    ├─ 输入验证
    │   ├─ 验证规则: {规则说明}
    │   └─ 配置参数: {param1} 控制校验开关
    ├─ 预处理
    │   ├─ 处理逻辑: {逻辑说明}
    │   └─ 指标: {metric1} Counter ↑
    └─ 输出: {中间数据格式A}
    ↓
[分支: 条件判断]
    ├─ 条件A: {判断逻辑}
    │   ↓
    │  [{子环节2A}: {类名2A}.{方法名2A}]
    │   ├─ 数据转换
    │   │   ├─ 转换规则: {规则说明}
    │   │   └─ 配置参数: {param2} 控制转换策略
    │   ├─ 业务计算
    │   │   ├─ 计算逻辑: {逻辑说明}
    │   │   └─ 指标: {metric2} Timer
    │   └─ 输出: {中间数据格式B1}
    │
    ├─ 条件B: {判断逻辑}
    │   ↓
    │  [{子环节2B}: {类名2B}.{方法名2B}] 【异步】
    │   ├─ 发送消息到 {消息队列/线程池}
    │   ├─ 超时: {timeout1}ms
    │   └─ 输出: {中间数据格式B2}
    │
    └─ 异常分支: {异常条件}
        ↓
       [{子环节2C}: 降级/异常处理]
        ├─ 降级策略: {降级说明}
        ├─ 指标: {metric_err} Counter ↑
        └─ 输出: {降级结果 / 错误响应}
    ↓
[{子环节3}: {类名3}.{方法名3}]
    ├─ 结果组装
    │   ├─ 组装逻辑: {逻辑说明}
    │   ├─ 关联外部数据: {外部依赖} (超时 {timeout2}ms)
    │   └─ 指标: {metric3} Gauge
    ├─ 最终校验
    │   └─ 校验规则: {规则说明}
    └─ 响应输出
    ↓
[数据输出: {输出格式}]
    │  字段: {field3}, {field4}
    │  去向: {下游环节}
```

#### 详细步骤

| 步骤 | 子环节 | 处理类/方法 | 处理内容 | 输入数据 | 输出数据 | 预期耗时 | 错误码 |
|-----|-------|------------|---------|---------|---------|---------|-------|
| 1 | {子环节1} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误码} |
| 2 | {子环节2} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误码} |
| 3 | {子环节3} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误码} |

#### 数据转换详情

**输入数据格式**：
```json
{
  "{field1}": {说明},
  "{field2}": {说明},
  "{field3}": {说明}
}
```

**中间数据格式（子环节1输出）**：
```json
{
  "{field4}": {说明},
  "{field5}": {说明}
}
```

**输出数据格式**：
```json
{
  "{field6}": {说明},
  "{field7}": {说明}
}
```

#### 状态机（如适用）

```
[初始状态] --{事件1}--> [状态1] --{事件2}--> [状态2] --{事件3}--> [终态]
                |                              |
                |--{事件4}--> [异常状态]       |--{事件5}--> [异常状态]
```

| 状态 | 状态码 | 含义 | 进入条件 | 可执行操作 |
|-----|-------|-----|---------|-----------|
| {状态1} | {code} | {含义} | {条件} | {操作} |
| {状态2} | {code} | {含义} | {条件} | {操作} |

#### 可引用的指标

**流程级指标**：
| 指标名 | 类型 | 采集点 | 说明 |
|-------|------|--------|------|
| {flow_metric1} | Counter | `{类}.{方法}` | {说明} |
| {flow_metric2} | Timer | `{类}.{方法}` | {说明} |

**各子环节指标**：
| 子环节 | 黄金指标 | 非黄金指标 |
|-------|---------|-----------|
| {子环节1} | {metric1}, {metric2} | {metric3}, {metric4} |
| {子环节2} | {metric5} | {metric6}, {metric7} |

#### 可引用的配置文件参数

| 参数名 | 默认值 | 配置路径 | 说明 | 影响范围 |
|-------|--------|---------|------|---------|
| {param1} | {value} | `{配置路径}` | {说明} | {影响} |
| {param2} | {value} | `{配置路径}` | {说明} | {影响} |

### 流程2：{流程名称}

#### 流程概述
- **业务目的**：{描述}
- **触发条件**：{条件}
- **参与子环节**：{子环节} → {子环节}
- **预期耗时**：{时间}

#### 流程图
```
[触发条件]
    ↓
[步骤1: {子环节}/{类}] → 处理: {内容}
    ↓
[步骤2: {子环节}/{类}] → 处理: {内容}
    ↓
[结果输出]
```

---

## 常见故障模式

### 故障模式1：{故障模式名称}

#### 故障概述
- **故障名称**：{名称}
- **故障等级**：{P0/P1/P2/P3}
- **发生频率**：{频繁/偶尔/罕见}
- **影响范围**：{范围说明}

#### 典型症状
{描述该故障的典型症状和表现，可以包括：}
- **用户感知**：{用户会看到什么}
- **系统表现**：{系统日志/监控表现}
- **数据表现**：{数据层面异常}

#### 关键指标特征

**黄金指标异常**：
| 指标 | 正常值 | 异常值 | 异常特征 |
|-----|-------|-------|---------|
| {metric1} | {正常} | {异常} | {特征描述} |
| {metric2} | {正常} | {异常} | {特征描述} |

**非黄金指标异常**：
| 指标 | 正常值 | 异常值 | 异常特征 |
|-----|-------|-------|---------|
| {aux_metric1} | {正常} | {异常} | {特征} |

#### 可能原因

1. **原因1：{原因名称}**
   - **概率**：{高/中/低}
   - **判断依据**：{如何判断是这个原因}
   - **根因分析**：{为什么会出现这个原因}

2. **原因2：{原因名称}**
   - **概率**：{高/中/低}
   - **判断依据**：{判断方法}
   - **根因分析**：{分析}

3. **原因3：{原因名称}**
   - **概率**：{高/中/低}
   - **判断依据**：{判断方法}

#### 排查建议

**快速排查（5分钟内）**：
1. 检查 `{metric1}` 指标趋势
2. 查看 {子环节1} 的日志关键字 `{keyword}`
3. 确认 {外部依赖} 的健康状态

**深度排查（15分钟内）**：
1. 分析 `{metric2}` 的分布特征
2. 检查 {子环节2} 的配置参数 `{param}`
3. 对比历史同期指标数据

**验证方法**：
- {如何验证问题已定位}
- {如何验证修复有效}

#### 关联子环节
| 子环节 | 关联说明 | 重点关注指标 |
|-------|---------|-------------|
| {子环节1} | {关联说明} | {metric1}, {metric2} |
| {子环节2} | {关联说明} | {metric3} |

#### 解决方案

**临时方案**：
- {快速止血方法}
- {降级策略}
- {预期恢复时间}

**长期方案**：
- {根本解决方法}
- {优化建议}
- {预防措施}

### 故障模式2：{故障模式名称}

#### 故障概述
- **故障名称**：{名称}
- **故障等级**：{P0/P1/P2/P3}

#### 典型症状
{描述}

#### 关键指标特征
- {metric1}：{异常特征}
- {metric2}：{异常特征}

#### 可能原因
1. {原因1}
2. {原因2}

#### 排查建议
{排查步骤}

---

## 限流与降级策略

### 限流策略

| 限流维度 | 阈值 | 触发条件 | 处理方式 | 影响范围 |
|---------|-----|---------|---------|---------|
| QPS限流 | {X}/s | {条件} | {拒绝/排队} | {范围} |
| 并发限流 | {X} | {条件} | {拒绝} | {范围} |
| 资源限流 | {X}% | {条件} | {降级} | {范围} |

### 降级策略

| 降级场景 | 触发条件 | 降级行为 | 恢复条件 |
|---------|---------|---------|---------|
| {场景1} | {条件} | {行为} | {条件} |
| {场景2} | {条件} | {行为} | {条件} |

---

## 下游关联

### 依赖的环节/服务

| 依赖项 | 依赖类型 | 调用方式 | 超时配置 | 重试策略 | 熔断配置 | 故障影响 |
|-------|---------|---------|---------|---------|---------|---------|
| {下游1} | {强/弱} | {同步/异步} | {连接}/{读取}ms | {次数}/{间隔} | {策略} | {影响} |
| {下游2} | {强/弱} | {同步/异步} | {超时}ms | {重试} | {熔断} | {影响} |

### 被依赖的环节/服务

| 上游项 | 依赖类型 | 调用方式 | SLA承诺 | 版本兼容 |
|-------|---------|---------|---------|---------|
| {上游1} | {强/弱} | {方式} | {SLA} | {兼容说明} |
| {上游2} | {强/弱} | {方式} | {SLA} | {兼容说明} |

### 依赖拓扑图

```
        [{上游1}] ──────┐
                       ↓
[{上游2}] ──同步调用──→ [{当前环节}] ──异步消息──→ [{下游1}]
                       ↓
        [{上游3}] ──事件驱动──┘           └─RPC调用──→ [{下游2}]
                                              ↓
                                              [{下游3}]
```

---

## 配置参数汇总

### 核心配置

| 参数名 | 默认值 | 配置路径 | 说明 | 调优建议 |
|-------|--------|---------|------|---------|
| {config1} | {value} | `{路径}` | {说明} | {建议} |
| {config2} | {value} | `{路径}` | {说明} | {建议} |
| {config3} | {value} | `{路径}` | {说明} | {建议} |

### 性能配置

| 参数名 | 默认值 | 推荐值 | 说明 |
|-------|--------|-------|------|
| {perf1} | {default} | {rec} | {说明} |
| {perf2} | {default} | {rec} | {说明} |

### 超时配置

| 参数名 | 默认值 | 说明 | 建议值 |
|-------|--------|------|-------|
| {timeout1} | {value} | {说明} | {建议} |
| {timeout2} | {value} | {说明} | {建议} |

---

## 相关文档索引

| 文档类型 | 文档路径 | 说明 |
|---------|---------|------|
| 父级文档 | `../整体架构.md` | 项目级架构定义 |
| 子环节1 | `{子环节1}.md` | 子环节1详细逻辑 |
| 子环节2 | `{子环节2}.md` | 子环节2详细逻辑 |
| 实体模型 | `../../03-实体模型/` | 相关实体定义 |
| 故障排查 | `../../04-场景能力/故障排查.md` | 排障SOP |
