>> **子 Agent 执行提示**：你当前是一个独立的子 Agent，专门负责**这一个环节**。你的上下文预算全部用于该环节，必须做到：1）用 `find` 命令探测源码目录；2）全量读取入口层 + 追踪调用链到边界；3）生成深度不低于模板示例的文档；4）写完后执行 `find` 命令校验文件数量和子环节对齐。
>> **指标体系元数据**：写稿前须结合主 Agent 下发的摘录，并自行用文件工具核对 `/app/upload/data`。**本文件与每个子环节 `.md` 均须包含** `## 黄金指标` 与 `## 非黄金指标`（非黄金为 **辅助指标** 列表，清单式呈现）。**禁止**使用 `## 监控指标`、自建的监控类宽表或「可观测性指标总览」等**与模板不一致**的标题来承载指标行；**唯一**合法分节名即**黄金/非黄金**。**指标 identifier** 须与元数据**逐字一致**，禁止截断。采集点用 **`类.方法`** 表述即可；无元数据命中时注明并标「源码推断」。

# {环节名称}整体逻辑

## 环节概述

### 基本信息
| 属性 | 内容 |
|-----|------|
| **环节名称** | {环节名称} |
| **所属系统** | {项目名} |
| **环节层级** | L3 - 环节级 |
| **模块归属** | {业务/包级语义名，如 order-service，不写具体文件路径} |
| **代表性入口** | `{主类名}.{主方法名}`（仅类.方法，用于定位逻辑，非文件路径） |

### 核心功能
{详细描述该环节的核心功能和主要职责，包括：}
- **主要责任**：{该环节负责的核心业务}
- **辅助责任**：{次要但相关的业务}
- **边界责任**：{明确不包含的职责，避免与相邻环节混淆}

### 职责范围
{详细说明该环节负责的具体业务范围或功能领域，可以包括：}
- **功能边界**：{该环节处理什么，不处理什么}
- **数据边界**：{该环节管理哪些数据，不管理哪些}
- **接口边界**：{提供什么接口，调用什么接口}

### 输入输出

#### 输入数据
| 输入名称 | 输入类型 | 来源环节/系统 | 数据格式 | 传输协议 | 频率 |
|---------|---------|-------------|---------|---------|-----|
| {输入1} | {请求/事件/数据} | {来源} | {JSON/Protobuf/...} | {HTTP/gRPC/MQ} | {实时/批量} |
| {输入2} | {类型} | {来源} | {格式} | {协议} | {频率} |

#### 输出数据
| 输出名称 | 输出类型 | 目标环节/系统 | 数据格式 | 传输协议 | 方式 |
|---------|---------|-------------|---------|---------|-----|
| {输出1} | {响应/事件/数据} | {目标} | {格式} | {协议} | {同步/异步} |
| {输出2} | {类型} | {目标} | {格式} | {协议} | {方式} |

### 在架构中的位置

```
[{上游环节1}] ──{数据流}──┐
                         ↓
[{上游环节2}] ──{数据流}──→ [{当前环节}] ──{数据流}──→ [{下游环节1}]
                         ↓                           ↓
                         └─{数据流}──→ [{下游环节2}] ──{数据流}──→ [{下游环节3}]
```

**上下游关系详表**：
| 关系 | 环节/系统 | 交互方式 | 交互内容 | 依赖强度 |
|-----|---------|---------|---------|---------|
| 上游 | {上游1} | {方式} | {内容} | {强/弱} |
| 上游 | {上游2} | {方式} | {内容} | {强/弱} |
| 下游 | {下游1} | {方式} | {内容} | {强/弱} |
| 下游 | {下游2} | {方式} | {内容} | {强/弱} |

---

## 黄金指标

> **本环节全量**；采用清单式（无大宽表）。**每一条 identifier 必须来自指标元数据**；禁止用 Redis `INFO` 字段名、监控短名等**自创**为行键。合法示例：`- \`redis.net.instantaneous_ops_per_sec\`：{说明，可注 INFO stats 等采集面}`；不合法：`- \`connected_clients\`：…` 若元数据实为 `redis.connected_clients`。

**黄金指标（1-5个）：**
- `{id_golden_1}`：{指标作用说明；需要时写出采集点 `{类名}.{方法名}`、阈值与告警}
- `{id_golden_2}`：{说明}
- `{id_golden_3}`：{说明}

## 非黄金指标

> **本环节全量**；**仅**保留与「黄金」并列的**辅助指标**列表，**禁止**再设业务/资源/性能等独立指标节。**辅助指标中的 identifier 同样必须来自元数据**（非黄金集合），禁止私自构造。

**辅助指标：**
- `{id_aux_1}`：{指标作用说明}
- `{id_aux_2}`：{说明}
- `{id_aux_3}`：{说明}

---

## 子环节与黄金指标

> **指标清单完整性**：各子环节 `.md` 中的 **黄金 / 非黄金（辅助指标）** 须覆盖划归本子环节的元数据条目（**identifier** 逐字一致）。若元数据将同一指标挂在多个子环节，可分别列示并注明「同指标多采集面」。用户可见文档中禁止出现内部元数据目录路径。

> **物理阻断式生成要求（核心约束）**：
> 本节表格中**每写一行子环节**，你都必须**立即停止继续写 `整体逻辑.md`**，转身去生成对应的 `{子环节}.md` 文件。只有该子环节的 `.md` 文件写完后，你才允许回到本文件继续写下一个子环节。
>
> **禁止行为**：
> - 禁止先把本节的 3-5 个子环节全部写完，再回头统一生成子环节文件
> - 禁止在表格中声明了子环节但同目录下没有对应的 `.md` 文件
> - 若暂未拆分子环节，写完本节后必须立刻在同一上下文中生成至少 2 份细节文档

### 子环节总览

| 子环节名称 | 核心职责 | 对应文档 | 复杂度 | 风险等级 |
|-----------|---------|---------|-------|---------|
| {子环节1} | {职责} | `{子环节1}.md` | {高/中/低} | {P0/P1/P2} |
| {子环节2} | {职责} | `{子环节2}.md` | {高/中/低} | {P0/P1/P2} |
| {子环节3} | {职责} | `{子环节3}.md` | {高/中/低} | {P0/P1/P2} |

### 子环节1：{子环节名称}

#### 职责说明
- **核心功能**：{详细描述}
- **业务价值**：{解决什么问题}
- **触发条件**：{什么时候执行}
- **执行频率**：{实时/定时/事件驱动}

#### 黄金指标

**黄金指标（1-5个）：**
- `{id_golden_1}`：{指标作用说明}
- `{id_golden_2}`：{说明}
- `{id_golden_3}`：{说明}

#### 对应文档
**详细文档**：`{子环节名称}.md`

### 子环节2：{子环节名称}

#### 职责说明
- **核心功能**：{描述}
- **业务价值**：{说明}
- **触发条件**：{条件}
- **执行频率**：{频率}

#### 黄金指标

**黄金指标（1-5个）：**
- `{id_golden_1}`：{指标作用说明}

#### 对应文档
**详细文档**：`{子环节名称}.md`

---

## 核心数据流程

### 流程1：{流程名称}

#### 流程概述
- **业务目的**：{该流程解决什么问题}
- **触发条件**：{什么时候触发}
- **参与子环节**：{子环节1} → {子环节2} → {子环节3}
- **预期耗时**：{正常情况下的完成时间}
- **超时时间**：{设置的超时阈值}

#### 流程图

> **流程深度要求**：禁止只画 3-5 个高层阶段框，必须拆解到「子环节内的关键操作/方法级别」，并显式标注分支、异常、指标、配置参数。

```
[数据输入: {输入格式}]
    │  字段: {field1}, {field2}
    │  来源: {上游环节}
    ↓
[{子环节1}: {类名}.{方法名}]
    ├─ 输入验证
    │   ├─ 验证规则: {规则说明}
    │   └─ 配置参数: {param1} 控制校验开关
    ├─ 预处理
    │   ├─ 处理逻辑: {逻辑说明}
    │   └─ 指标: {metric1} Counter ↑
    └─ 输出: {中间数据格式A}
    ↓
[分支: 条件判断]
    ├─ 条件A: {判断逻辑}
    │   ↓
    │  [{子环节2A}: {类名2A}.{方法名2A}]
    │   ├─ 数据转换
    │   │   ├─ 转换规则: {规则说明}
    │   │   └─ 配置参数: {param2} 控制转换策略
    │   ├─ 业务计算
    │   │   ├─ 计算逻辑: {逻辑说明}
    │   │   └─ 指标: {metric2} Timer
    │   └─ 输出: {中间数据格式B1}
    │
    ├─ 条件B: {判断逻辑}
    │   ↓
    │  [{子环节2B}: {类名2B}.{方法名2B}] 【异步】
    │   ├─ 发送消息到 {消息队列/线程池}
    │   ├─ 超时: {timeout1}ms
    │   └─ 输出: {中间数据格式B2}
    │
    └─ 异常分支: {异常条件}
        ↓
       [{子环节2C}: 降级/异常处理]
        ├─ 降级策略: {降级说明}
        ├─ 指标: {metric_err} Counter ↑
        └─ 输出: {降级结果 / 错误响应}
    ↓
[{子环节3}: {类名3}.{方法名3}]
    ├─ 结果组装
    │   ├─ 组装逻辑: {逻辑说明}
    │   ├─ 关联外部数据: {外部依赖} (超时 {timeout2}ms)
    │   └─ 指标: {metric3} Gauge
    ├─ 最终校验
    │   └─ 校验规则: {规则说明}
    └─ 响应输出
    ↓
[数据输出: {输出格式}]
    │  字段: {field3}, {field4}
    │  去向: {下游环节}
```

#### 详细步骤

| 步骤 | 子环节 | 处理类/方法 | 处理内容 | 输入数据 | 输出数据 | 预期耗时 | 错误码 |
|-----|-------|------------|---------|---------|---------|---------|-------|
| 1 | {子环节1} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误码} |
| 2 | {子环节2} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误码} |
| 3 | {子环节3} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误码} |

#### 数据转换详情

**输入数据格式**：
```json
{
  "{field1}": {说明},
  "{field2}": {说明},
  "{field3}": {说明}
}
```

**中间数据格式（子环节1输出）**：
```json
{
  "{field4}": {说明},
  "{field5}": {说明}
}
```

**输出数据格式**：
```json
{
  "{field6}": {说明},
  "{field7}": {说明}
}
```

#### 状态机（如适用）

```
[初始状态] --{事件1}--> [状态1] --{事件2}--> [状态2] --{事件3}--> [终态]
                |                              |
                |--{事件4}--> [异常状态]       |--{事件5}--> [异常状态]
```

| 状态 | 状态码 | 含义 | 进入条件 | 可执行操作 |
|-----|-------|-----|---------|-----------|
| {状态1} | {code} | {含义} | {条件} | {操作} |
| {状态2} | {code} | {含义} | {条件} | {操作} |

#### 可引用的指标

> 仅列举本流程中出现的 **指标 identifier**，且须与上文 **黄金指标 / 非黄金指标** 或子环节文档已定义条目一致（**禁止**在此新增未归类为黄金/非黄金的指标类型）。

- `{id_golden_1}` 或 `{id_aux_1}`：{在本流程节点中的用途；采集点 `{类}.{方法}`}

#### 可引用的配置文件参数

| 参数名 | 默认值 | 配置路径 | 说明 | 影响范围 |
|-------|--------|---------|------|---------|
| {param1} | {value} | `{配置路径}` | {说明} | {影响} |
| {param2} | {value} | `{配置路径}` | {说明} | {影响} |

### 流程2：{流程名称}

#### 流程概述
- **业务目的**：{描述}
- **触发条件**：{条件}
- **参与子环节**：{子环节} → {子环节}
- **预期耗时**：{时间}

#### 流程图
```
[触发条件]
    ↓
[步骤1: {子环节}/{类}] → 处理: {内容}
    ↓
[步骤2: {子环节}/{类}] → 处理: {内容}
    ↓
[结果输出]
```

---

## 常见故障模式

> 常见故障模式采用固定小节：**典型症状**、**关键指标特征**（仅围绕**黄金/非黄金** identifier）、**可能原因**、**排查建议**。

### 故障模式1：{故障模式名称}

**典型症状：**
{描述该故障的典型症状和表现}

**关键指标特征：**
- `{id_golden_1}`：{该黄金指标在出现此故障时的异常特征}
- `{id_golden_2}`：{…}
- `{id_aux_1}`：{非黄金指标在此故障下的异常特征（若适用）}

**可能原因：**
1. {原因1}
2. {原因2}

**排查建议：**
{给出具体排查步骤，重点关注上文黄金/非黄金指标与相关子环节}

### 故障模式2：{故障模式名称}

**典型症状：**
{描述}

**关键指标特征：**
- `{id_golden_1}`：{异常特征}
- `{id_aux_1}`：{异常特征}

**可能原因：**
1. {原因1}
2. {原因2}

**排查建议：**
{排查步骤}

---

## 限流与降级策略

### 限流策略

| 限流维度 | 阈值 | 触发条件 | 处理方式 | 影响范围 |
|---------|-----|---------|---------|---------|
| QPS限流 | {X}/s | {条件} | {拒绝/排队} | {范围} |
| 并发限流 | {X} | {条件} | {拒绝} | {范围} |
| 资源限流 | {X}% | {条件} | {降级} | {范围} |

### 降级策略

| 降级场景 | 触发条件 | 降级行为 | 恢复条件 |
|---------|---------|---------|---------|
| {场景1} | {条件} | {行为} | {条件} |
| {场景2} | {条件} | {行为} | {条件} |

---

## 下游关联

### 依赖的环节/服务

| 依赖项 | 依赖类型 | 调用方式 | 超时配置 | 重试策略 | 熔断配置 | 故障影响 |
|-------|---------|---------|---------|---------|---------|---------|
| {下游1} | {强/弱} | {同步/异步} | {连接}/{读取}ms | {次数}/{间隔} | {策略} | {影响} |
| {下游2} | {强/弱} | {同步/异步} | {超时}ms | {重试} | {熔断} | {影响} |

### 被依赖的环节/服务

| 上游项 | 依赖类型 | 调用方式 | SLA承诺 | 版本兼容 |
|-------|---------|---------|---------|---------|
| {上游1} | {强/弱} | {方式} | {SLA} | {兼容说明} |
| {上游2} | {强/弱} | {方式} | {SLA} | {兼容说明} |

### 依赖拓扑图

```
        [{上游1}] ──────┐
                       ↓
[{上游2}] ──同步调用──→ [{当前环节}] ──异步消息──→ [{下游1}]
                       ↓
        [{上游3}] ──事件驱动──┘           └─RPC调用──→ [{下游2}]
                                              ↓
                                              [{下游3}]
```

---

## 配置参数汇总

### 核心配置

| 参数名 | 默认值 | 配置路径 | 说明 | 调优建议 |
|-------|--------|---------|------|---------|
| {config1} | {value} | `{路径}` | {说明} | {建议} |
| {config2} | {value} | `{路径}` | {说明} | {建议} |
| {config3} | {value} | `{路径}` | {说明} | {建议} |

### 性能配置

| 参数名 | 默认值 | 推荐值 | 说明 |
|-------|--------|-------|------|
| {perf1} | {default} | {rec} | {说明} |
| {perf2} | {default} | {rec} | {说明} |

### 超时配置

| 参数名 | 默认值 | 说明 | 建议值 |
|-------|--------|------|-------|
| {timeout1} | {value} | {说明} | {建议} |
| {timeout2} | {value} | {说明} | {建议} |

---

## 相关文档索引

| 文档类型 | 文档路径 | 说明 |
|---------|---------|------|
| 父级文档 | `../整体架构.md` | 项目级架构定义 |
| 子环节1 | `{子环节1}.md` | 子环节1详细逻辑 |
| 子环节2 | `{子环节2}.md` | 子环节2详细逻辑 |
| 实体模型 | `../../03-实体模型/` | 相关实体定义 |
| 故障排查 | `../../04-场景能力/故障排查.md` | 排障SOP |
