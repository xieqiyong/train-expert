# {项目名}整体架构

## 架构概述

### 架构类型与特点
- **架构类型**：{微服务架构 / 单体架构 / 事件驱动架构 / 分层架构等}
- **部署模式**：{集群部署 / 单实例部署 / 多活部署 / 容器化部署}
- **核心特点**：
  - {特点1：如高可用设计 - 多副本 + 故障自动转移}
  - {特点2：如高性能设计 - 异步处理 + 缓存加速}
  - {特点3：如可扩展设计 - 水平扩展 + 插件化架构}

### 版本信息
- **当前版本**：{版本号}
- **发布日期**：{日期}
- **与上一版本的架构变化**：
  - {变化1：如新增了某某模块}
  - {变化2：如重构了某某流程}
  - {变化3：如引入了某某技术}

---

## 技术栈详单

### 语言与框架
| 层级 | 技术选型 | 版本 | 用途说明 |
|-----|---------|------|---------|
| 编程语言 | {如 Java} | {如 17} | 核心业务逻辑实现 |
| 主框架 | {如 Spring Boot} | {如 3.2.x} | 应用基础框架 |
| Web层 | {如 Spring MVC / WebFlux} | {版本} | HTTP请求处理 |
| 数据访问 | {如 MyBatis Plus / JPA} | {版本} | 数据库访问 |
| 安全框架 | {如 Spring Security / Shiro} | {版本} | 认证鉴权 |

### 中间件与存储
| 类型 | 技术选型 | 版本 | 部署方式 | 用途说明 |
|-----|---------|------|---------|---------|
| 关系型数据库 | {如 MySQL / PostgreSQL} | {版本} | {主从/集群} | 业务数据持久化 |
| 缓存 | {如 Redis / Caffeine} | {版本} | {集群/本地} | 热点数据缓存 |
| 消息队列 | {如 Kafka / RabbitMQ} | {版本} | {集群} | 异步消息通信 |
| 搜索引擎 | {如 Elasticsearch} | {版本} | {集群} | 全文检索 |
| 时序数据库 | {如 InfluxDB / OpenGemini} | {版本} | {集群} | 指标数据存储 |
| 注册中心 | {如 Nacos / Consul} | {版本} | {集群} | 服务发现与配置 |

### 运维与监控
| 类型 | 技术选型 | 用途说明 |
|-----|---------|---------|
| 日志收集 | {如 ELK / Loki} | 集中式日志管理 |
| 监控告警 | {如 Prometheus / Grafana} | 指标采集与可视化 |
| 链路追踪 | {如 SkyWalking / Jaeger} | 分布式链路追踪 |
| 容器编排 | {如 Kubernetes / Docker Swarm} | 容器生命周期管理 |

---

## 部署架构

### 部署拓扑图

```
                              [用户请求]
                                  ↓
                        [负载均衡器 / Nginx]
                                  ↓
              ┌───────────────────┼───────────────────┐
              ↓                   ↓                   ↓
        [API网关实例1]      [API网关实例2]      [API网关实例3]
              ↓                   ↓                   ↓
              └───────────────────┼───────────────────┘
                                  ↓
                    ┌─────────────┴─────────────┐
                    ↓                           ↓
            [服务集群A - 核心]            [服务集群B - 扩展]
            ┌─────────┬─────────┐          ┌─────────┐
            ↓         ↓         ↓          ↓
        [实例A1]  [实例A2]  [实例A3]    [实例B1]  [实例B2]
            └─────────┴─────────┘          └─────────┘
                    ↓                           ↓
              [数据层集群]                [缓存集群]
            ┌─────────┬─────────┐          ┌─────────┐
            ↓         ↓         ↓          ↓         ↓
        [MySQL主] [MySQL从1] [MySQL从2] [Redis主] [Redis从]
```

### 部署环境配置
| 环境 | 实例数 | 配置规格 | 部署位置 | 特殊配置 |
|-----|-------|---------|---------|---------|
| 生产环境 | {N} | {CPU}核/{内存}GB/{磁盘}GB | {可用区/数据中心} | {高可用/灾备配置} |
| 预发环境 | {N} | {CPU}核/{内存}GB | {可用区} | {配置说明} |
| 测试环境 | {N} | {CPU}核/{内存}GB | {可用区} | {配置说明} |

### 网络架构
- **服务网格**：{是否使用 Istio/Linkerd 等服务网格}
- **服务间通信**：{HTTP REST / gRPC / 消息队列}
- **外部依赖通信**：{同步调用 / 异步回调 / Webhook}
- **网络安全**：{TLS版本 / 证书管理 / 网络隔离策略}

---

## 目录结构

### 源码目录组织

```
{项目根目录}/
├── src/
│   ├── main/
│   │   ├── java/com/{公司名}/{项目名}/
│   │   │   ├── api/                    # 对外接口层
│   │   │   │   ├── controller/         # REST API控制器
│   │   │   │   ├── dto/                # 数据传输对象
│   │   │   │   └── vo/                 # 视图对象
│   │   │   │
│   │   │   ├── service/                # 业务逻辑层
│   │   │   │   ├── impl/               # 服务实现
│   │   │   │   └── {领域1}Service.java
│   │   │   │
│   │   │   ├── domain/                 # 领域层
│   │   │   │   ├── {领域1}/            # 领域包
│   │   │   │   │   ├── entity/         # 实体定义
│   │   │   │   │   ├── repository/     # 仓储接口
│   │   │   │   │   ├── event/          # 领域事件
│   │   │   │   │   └── service/        # 领域服务
│   │   │   │   └── {领域2}/
│   │   │   │
│   │   │   ├── infrastructure/         # 基础设施层
│   │   │   │   ├── config/             # 配置类
│   │   │   │   ├── persistence/        # 持久化实现
│   │   │   │   ├── messaging/          # 消息队列实现
│   │   │   │   └── cache/              # 缓存实现
│   │   │   │
│   │   │   ├── common/                 # 公共组件
│   │   │   │   ├── util/               # 工具类
│   │   │   │   ├── exception/          # 异常定义
│   │   │   │   └── constants/          # 常量定义
│   │   │   │
│   │   │   └── job/                    # 定时任务
│   │   │       ├── {任务1}Job.java
│   │   │       └── {任务2}Job.java
│   │   │
│   │   └── resources/
│   │       ├── application.yml         # 主配置
│   │       ├── application-{profile}.yml # 环境配置
│   │       ├── mapper/                 # MyBatis映射文件
│   │       └── sql/                    # SQL脚本
│   │
│   └── test/                           # 测试代码
│
├── config/                             # 外部配置
├── docs/                               # 文档
├── scripts/                            # 脚本文件
└── docker/                             # Docker配置
```

### 关键文件说明

| 文件路径 | 说明 | 重要性 | 变更影响 |
|---------|------|-------|---------|
| {文件1路径} | {说明} | ⭐⭐⭐ | {影响范围} |
| {文件2路径} | {说明} | ⭐⭐ | {影响范围} |
| {文件3路径} | {说明} | ⭐⭐⭐ | {影响范围} |

---

## 系统依赖关系

### 外部依赖（Upstream）

| 依赖系统 | 依赖类型 | 接口协议 | 调用方式 | 超时配置 | 熔断策略 | 故障影响 |
|---------|---------|---------|---------|---------|---------|---------|
| {系统A} | 强依赖 | {REST/gRPC} | 同步调用 | {连接超时}ms / {读取超时}ms | {熔断器配置} | {故障时影响说明} |
| {系统B} | 弱依赖 | {消息队列} | 异步消费 | {消费超时}ms | {重试策略} | {故障时影响说明} |
| {系统C} | 数据依赖 | {数据库协议} | 连接池 | {连接超时}ms | {连接池配置} | {故障时影响说明} |

### 被依赖关系（Downstream）

| 消费者系统 | 消费内容 | 消费方式 | SLA要求 | 版本兼容性 |
|-----------|---------|---------|---------|-----------|
| {系统X} | {数据/服务} | {同步/异步} | {可用性要求} | {版本兼容说明} |
| {系统Y} | {数据/服务} | {同步/异步} | {可用性要求} | {版本兼容说明} |

### 依赖拓扑图

```
[外部系统A] ──同步调用──→ [本系统] ──异步消息──→ [外部系统B]
                              ↓
                        [外部系统C]
                        (数据存储)
```

---

## 环节与黄金指标

> **重要说明**：本节定义的每个环节必须在同目录下有对应的 `{环节}/` 文件夹，每个环节的黄金指标用于故障排查时的全局扫描。

### 环节定义总览

> **环节识别约束**：环节列表必须基于 `static_package/` 的源码结构全面识别，禁止遗漏核心模块，但也禁止将基础设施/工具包列为环节。识别原则：
> - **哪些应该列为环节**：
>   - 按源码目录中的**一级/二级业务包名**映射（如 `com.databuff.webapp.apm` → APM 环节）
>   - 每个**独立业务领域**必须被定义为一个环节（如链路追踪、告警管理、RUM、指标查询各自为独立环节）
>   - 每个**独立技术组件**有独立入口和核心逻辑的，必须单独定义环节
> - **哪些禁止列为环节（纯基础设施/通用包）**：
>   - 通用工具包：`util`、`utils`、`common`、`helper`、`tools`
>   - 配置/常量/异常定义：`config`、`configuration`、`constants`、`constant`、`exception`、`exceptions`、`enums`
>   - 无业务入口的纯接口/抽象层：`abstracts`、`interfaces`（纯接口定义）
> - **禁止合并**：不能将 "APM + RUM + 告警" 合并为一个大环节，如果有独立的源码目录和 API 入口，必须分别定义
> - **双向校验**：本表列出的每个环节必须在同目录下存在 `{环节}/` 文件夹；同目录下的每个 `{环节}/` 文件夹必须在本表中有定义

| 环节名称 | 核心职责 | 关键依赖 | 风险等级 | 对应源码目录 | 对应文档目录 |
|---------|---------|---------|---------|-------------|-------------|
| {环节1} | {职责说明} | {依赖} | {高/中/低} | `static_package/src/.../{包名1}/` | `{环节1}/` |
| {环节2} | {职责说明} | {依赖} | {高/中/低} | `static_package/src/.../{包名2}/` | `{环节2}/` |
| {环节3} | {职责说明} | {依赖} | {高/中/低} | `static_package/src/.../{包名3}/` | `{环节3}/` |

### 环节1：{环节名称}

#### 职责说明
- **核心功能**：{描述该环节的主要职责}
- **业务范围**：{详细说明业务范围}
- **输入来源**：{数据/请求从哪来}
- **输出去向**：{数据/结果到哪去}
- **性能要求**：{RT要求 / 吞吐量要求 / 可用性要求}

#### 黄金指标（故障排查用）

| 指标字段名 | 指标作用说明 | 采集点 | 正常范围 | 异常判定 | 告警级别 |
|-----------|-------------|-------|---------|---------|---------|
| {metric1} | {说明} | `{类名}:{方法名}` | {范围} | {判定标准} | {P0/P1/P2} |
| {metric2} | {说明} | `{类名}:{方法名}` | {范围} | {判定标准} | {P0/P1/P2} |
| {metric3} | {说明} | `{类名}:{方法名}` | {范围} | {判定标准} | {P0/P1/P2} |

#### 容量规划
- **设计峰值**：{QPS/TPS}
- **当前峰值**：{QPS/TPS}
- **扩容阈值**：{触发扩容的指标阈值}
- **资源限制**：{CPU/内存/连接数限制}

#### 故障影响
- **故障场景**：{该环节故障时的表现}
- **影响范围**：{影响哪些下游系统/功能}
- **降级策略**：{故障时的降级方案}
- **恢复时间目标(RTO)**：{X}分钟
- **恢复点目标(RPO)**：{X}分钟

### 环节2：{环节名称}

#### 职责说明
- **核心功能**：{描述}
- **业务范围**：{详细说明}
- **输入来源**：{数据来源}
- **输出去向**：{数据去向}
- **性能要求**：{性能指标}

#### 黄金指标

| 指标字段名 | 指标作用说明 | 采集点 | 正常范围 | 异常判定 | 告警级别 |
|-----------|-------------|-------|---------|---------|---------|
| {metric1} | {说明} | `{类名}:{方法名}` | {范围} | {判定标准} | {P0/P1/P2} |
| {metric2} | {说明} | `{类名}:{方法名}` | {范围} | {判定标准} | {P0/P1/P2} |

#### 容量规划
- **设计峰值**：{QPS/TPS}
- **当前峰值**：{QPS/TPS}
- **扩容阈值**：{触发条件}

#### 故障影响
- **故障场景**：{表现}
- **影响范围**：{影响说明}
- **降级策略**：{降级方案}

---

## 核心数据流程

### 流程1：{流程名称}

#### 流程概述
- **业务场景**：{描述该流程解决的业务问题}
- **触发条件**：{什么情况下触发该流程}
- **参与环节**：{环节1} → {环节2} → {环节3}
- **整体时延**：{预期完成时间}

#### 详细流程

```
[数据输入]
    ↓ {输入格式/协议}
[{环节1}]
    ├─ 处理1: {处理内容}
    ├─ 处理2: {处理内容}
    ↓ {输出格式}
[{环节2}]
    ├─ 处理1: {处理内容}
    ↓ {输出格式}
[{环节3}]
    └─ 处理: {处理内容}
    ↓
[数据输出]
```

#### 步骤详单

| 步骤 | 所在环节 | 处理类/方法 | 处理内容 | 输入 | 输出 | 预期耗时 | 错误处理 |
|-----|---------|------------|---------|-----|-----|---------|---------|
| 1 | {环节1} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误处理} |
| 2 | {环节2} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误处理} |
| 3 | {环节3} | `{类}.{方法}` | {描述} | {输入} | {输出} | {X}ms | {错误处理} |

#### 数据转换

| 阶段 | 数据格式 | 关键字段 | 字段说明 |
|-----|---------|---------|---------|
| 输入 | {格式} | {字段列表} | {说明} |
| 环节1输出 | {格式} | {字段列表} | {说明} |
| 环节2输出 | {格式} | {字段列表} | {说明} |
| 最终输出 | {格式} | {字段列表} | {说明} |

#### 可引用的指标
- **黄金指标**：
  - `{metric1}`：{在此流程中的作用}
  - `{metric2}`：{在此流程中的作用}
- **流程专属指标**：
  - `{flow_metric1}`：{说明}

#### 可引用的配置参数

| 参数名 | 默认值 | 配置路径 | 说明 | 调优建议 |
|-------|--------|---------|------|---------|
| {param1} | {value} | `{配置路径}` | {说明} | {建议} |
| {param2} | {value} | `{配置路径}` | {说明} | {建议} |

### 流程2：{流程名称}

#### 流程概述
- **业务场景**：{描述}
- **触发条件**：{条件}
- **参与环节**：{环节} → {环节}
- **整体时延**：{时间}

#### 详细流程
```
[触发条件]
    ↓
[步骤1: {环节}/{类}] → 处理: {内容}
    ↓
[步骤2: {环节}/{类}] → 处理: {内容}
    ↓
[结果输出]
```

---

## 环节-子环节映射索引

> 用于快速定位子环节文档

| 环节 | 子环节 | 子环节职责 | 对应文档 | 黄金指标数 |
|------|--------|-----------|----------|-----------|
| {环节1} | {子环节1} | {职责} | `{环节1}/{子环节1}.md` | {N}个 |
| {环节1} | {子环节2} | {职责} | `{环节1}/{子环节2}.md` | {N}个 |
| {环节2} | {子环节1} | {职责} | `{环节2}/{子环节1}.md` | {N}个 |
| {环节2} | {子环节2} | {职责} | `{环节2}/{子环节2}.md` | {N}个 |

---

## 全局配置参数

### 系统级配置

| 参数名 | 默认值 | 配置路径 | 说明 | 影响范围 |
|-------|--------|---------|------|---------|
| {global.param1} | {value} | `{路径}` | {说明} | {影响} |
| {global.param2} | {value} | `{路径}` | {说明} | {影响} |

### 性能调优配置

| 参数名 | 默认值 | 推荐值 | 说明 | 调优建议 |
|-------|--------|-------|------|---------|
| {perf.param1} | {default} | {recommended} | {说明} | {建议} |
| {perf.param2} | {default} | {recommended} | {说明} | {建议} |

### 安全配置

| 参数名 | 默认值 | 配置路径 | 说明 |
|-------|--------|---------|------|
| {security.param1} | {value} | `{路径}` | {说明} |

---

## 故障模式总览

> 各环节详细故障模式见 `{环节}/整体逻辑.md`

| 故障模式 | 关联环节 | 典型症状 | 黄金指标特征 | 紧急程度 |
|---------|---------|---------|-------------|---------|
| {故障1} | {环节1} | {症状} | {指标特征} | {P0/P1} |
| {故障2} | {环节2} | {症状} | {指标特征} | {P1/P2} |
