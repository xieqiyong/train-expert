## 核心运行说明

本文件用于进行故障定位。

**依赖文档**（相对当前版本目录）：
- `02-领域认知/整体架构.md`：项目整体架构及黄金指标定义
- `02-领域认知/{环节}/整体逻辑.md`：环节的整体逻辑及非黄金指标
- `02-领域认知/{环节}/{子环节}.md`：子环节逻辑细节
- `static_package/`：**在已按下文八步法则推进的前提下**，若仍需**更深一层明确业务/实现逻辑**，且仅依赖上述认知文档与当时指标/数据下钻**仍不足**时，可读取以补充认知；**不**用读源码替代或跳过任一步

**执行原则**：
- **默认优先八步法则**：下文的八步法则是排障的**主路径**，须完整、按序执行，记录步数，不得跳跃或提前下结论
- **源码为补充**：在八步流程内，**当**需要更深一层明确逻辑、且 `02-领域认知/`、`03-实体模型/` 与当步所需数据**仍无法支撑**该步判定时，再读取同版本 `static_package/` 补充，**不改变**八步顺序与「共八步」的强制要求
- 以下所有文档路径、架构知识和 `static_package/` 的读取范围，都默认相对于该版本目录
- 排障路径必须在走完当前 Skill 逻辑后，才能补充通用知识

---

## 核心运行说明

本技能用于进行故障定位。

### 运行流程

**八步法则**
- **必须强制执行**
- **每一步都不能少，需要计数，每走一步+1，必须保证最终是9**
- **开始、中间、结束必须记录已分析的步骤、未分析的步骤**

#### 第一步：明确查询时间
- 如果已经有queryStartTime、queryEndTime，则使用 queryStartTime、queryEndTime
- 如果没有，调用工具 `getCurrentTimeRange` 方法获取当前查询时间范围

#### 第二步：加载配置
- 如果能获取到当前项目的配置文件则加载它（如果配置文件为空则代表没有采集对应的配置文件则忽略）
- 配置文件用于补充后续的运行逻辑细节或者数据

#### 第三步：全局扫描
- 加载 `整体架构.md`，提取所有环节的【黄金指标】

#### 第四步：数据获取

- 找出所有环节的黄金指标
- 构建queryAndDrillDown的查询请求：
  - AbnormalDetail：由之前的分析结果中提取，如果没有则填null，不要随意编造
  - 构建MetricRequest
    - identifiers：即上述所有环节的黄金指标
    - queryStartTime：上述queryStartTime
    - queryEndTime：上述queryEndTime

#### 第五步：挑出有问题的环节

- 使用queryAndDrillDown工具分析黄金指标，找出有问题的黄金指标
  - 列出各个指标在 [abnormalStartTime, abnormalEndTime] 内的波动幅度
  - **指标验证(必须严格执行)**：指标必须在 `[abnormalStartTime, abnormalEndTime]` 内有吻合的波动时间点才算有问题的指标
  - 有问题的指标中rootCaseMaps作为有问题的维度，作为补充的细节信息
- 若某环节黄金指标异常则该环节标记为有问题
- **tag 维度下钻（标记异常后执行）**：对每个已判定异常的 **identifier**，调用 **`findMetricCoreByIdentifier`** 读取 MetricsCore；据合法 tag 选定 **`groupBys`**；在同一 **`[abnormalStartTime, abnormalEndTime]`**、同一 entity（及若有则同一 AbnormalDetail）下再次 **`queryAndDrillDown`**（传入 **`groupBys`**，依据 **`rootCaseMaps`** 归纳主导异常的维度取值并写入结论）
- 异常指标下的rootCaseMaps
  - 如果是null代表当前指标的问题并不集中在某几个tag维度上而是所有维度都有这种问题
  - 如果不是null代表当前指标的问题集中在rootCaseMaps对应的维度上
- **执行闸门（硬约束）**：凡是已标记异常的 **identifier**，若未完成上述 tag 下钻（未成功获取 MetricsCore、未形成有效 `groupBys`、或未执行带 `groupBys` 的二次 `queryAndDrillDown`）则本次分析无效，禁止进入结论推演与最终结论输出

#### 第六步：环节的详细分析

- 对有问题的环节，加载对应的 `整体逻辑.md`，提取出该环节的非黄金指标
- 按照第四步的方式构建查询请求，来查询非黄金指标以及进行下钻
  - **tag 维度下钻（标记异常后执行）**：对每个已判定异常的 **identifier**，调用 **`findMetricCoreByIdentifier`** 读取 MetricsCore；据合法 tag 选定 **`groupBys`**；在同一 **`[abnormalStartTime, abnormalEndTime]`**、同一 entity（及若有则同一 AbnormalDetail）下再次 **`queryAndDrillDown`**（传入 **`groupBys`**，依据 **`rootCaseMaps`** 归纳主导异常的维度取值并写入结论）
- **多级下钻逻辑**：
  - 若涉及子环节（如内存管理 -> 内存回收），必须先获取子环节指标再推导。
  - **因果链条建立**：必须描述“因为指标 A 突变导致了指标 B 的异常”，严禁孤立看待指标。
- **最终验证(必须严格执行)**：所有推导出的原因，其对应指标必须在 `[abnormalStartTime, abnormalEndTime]` 内有吻合的波动时间点。

#### 第七步：结论推演

##### 因果链条生成
- 仅基于已识别的异常指标，推理 1～3 条因果链条
- 每条链条需包含：根因指标 →（可选）中间指标 → 结果指标

##### 根因判定（硬约束）
- 根因指标必须在 `[abnormalStartTime, abnormalEndTime]` 内发生突变
- 突变需为数值级异常（突升 / 突降 / 结构性变化）
- 若黄金指标在该时间窗内均无突变：
  - **直接输出：`未发现异常`**
  - **禁止假设根因**

##### 结论推演
- 从最可能的因果链条开始
- 由因到果逐层验证下游指标是否符合预期

##### 因果校验（必须全部满足）
- 机制合理
- 时间顺序正确
- 符合系统架构
- 无更合理替代解释

##### 结论修正
- 任一校验失败 → 放弃该链条，推演下一条
- 全部链条失败 → 输出：`未发现异常`

##### 输出约束
- 最终仅允许一个结论
- 禁止多根因、模糊或猜测性结论
- 给出优化建议

### 第八步：决策是否下钻到宿主机

如果最终根因是资源层的问题，则需要继续下钻到宿主机继续进行资源分析

如果{项目名}分析已经找到**明确的组件级根因**（如配置错误、代码bug、逻辑问题），且与资源层无关，则：
- **不需要下钻**
- 直接输出最终结论和优化建议

### 一次性输出要求

- 明确要求 LLM 在获取数据后，在内部完成所有逻辑推理
- 严禁输出"准备进入下一步"等过渡性语言
- 一次性输出完整分析结果

### 重要约束

1. **严格遵循SOP**：严格遵守7步的分析步骤，所有的排查必须以.md文档为标准作业程序
2. **禁止跳跃**：在Skill路径未走完前，不得直接给出基于通用经验的结论
3. **精准权衡**：
  - 若Skill逻辑命中：直接给出Skill中的优化建议，并引用文档原文
  - 若Skill逻辑未命中：先说明"已排查完私有知识库，未发现匹配项"，再补充通用知识
4. **逐行对照**：代码逻辑分析时，逐行对照md中的逻辑说明，不进行脑补
5. **时间展示**：不能是一个数值，需要调用 formatTime(时间戳数值) 工具，如果时间戳转换已经出现过则直接复用，不要频繁重复调用工具


## 注意事项

- ❌ 禁止输出"准备进入下一步"、"现在开始分析"等过渡性语言
- ❌ 禁止在获取所有数据后分步骤输出，必须一次性输出完整报告
- ❌ 禁止在黄金指标正常时加载对应环节的MD文件
- ❌ 避免询问用户明确信息，自行创建分析思路
- ✅ 严格遵循.md文档作为SOP
- ✅ 逐行对照md文档中的运行逻辑，不进行脑补
- ✅ 精准权衡Skill逻辑和通用知识
- ✅ 所有逻辑推理在内部完成，一次性输出最终分析报告
- ✅ **时间展示**：不能是一个数值，需要调用 formatTime(时间戳数值) 工具，如果时间戳转换已经出现过则直接复用，不要频繁重复调用工具

